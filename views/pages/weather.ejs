<!DOCTYPE html>
<html lang="en">
	<head>
		<% include ../partials/head %>
		<!-- <% /* include ../partials/fb-pixels */ %> -->
	</head>
	<body class="container-fluid text-left">
		<div id="fb-root"></div>
		<script type="text/javascript" src="/scripts/facebook-integration.js"></script>
		<% include ../partials/header %>
		<main class="container-fluid">
			<div class="row">
				<section class="col-sm-4 well-sm weather-data-widget">
					<article class="row">
						<figure class="col-sm-4">
							<img class="pull-left img-responsive" src="../img/app-logo.png" title="application logo" />
						</figure>
						<div class="col-sm-8">
							<div class="row">
								<h3 id="fb-welcome"></h3>
								<h1 id="fb-location"></h1>
								<div class="text-left"><%= weda.name %> weather served below</div>
							</div>
							<div class="row"><% include ../partials/fb-like %></div>
						</div>
					</article>
					<hr />
					<article class="row text-left">
					<div class="col-sm-12">
						<p>Ground Temperature is <%= weda.main.temp %> &deg;C. With <%= weda.clouds.all %>% of the sky covered with <%= weda.weather[0].description%>.</p>
						<p>Wind averages <%= weda.wind.speed %> km/h blowing in from <%= weda.wind.deg %> &deg;North</p>
						<p>Atmospheric pressure is <%= weda.main.pressure %> hPa and <%= weda.main.humidity %>% humidity.</p>
					</div>
					</article>
					<article class="row">
						<section class="col-sm-12">
							<table class="table">
								<thead>
									<tr>
										<th>latitude</th>
										<th>longitude</th>
										<th>sea level</th>
										<th>sunrises</th>
										<th>sunsets</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td><%= weda.coord.lat %></td>
										<td><%= weda.coord.lon %></td>
										<td><%= weda.main.sea_level %></td>
										<td class="date-sunrise"><%= weda.sys.sunrise %></td>
										<td class="date-sunset"><%= weda.sys.sunset %></td>
									</tr>
								</tbody>
							</table>
						</section>
					</article>
					<article class="row">
						<form class="form-inline navbar-form" action="/api/" method="get" name="form_citydetailsbyname" id="form_citydetailsbyname" enctype="multipart/form-data" >
							<input id="f_elem_city_name" class="form-control" type="text" name="city" placeholder="City Name">
							<button class="btn btn-success-outline" required title="Get city data" type="submit"><i class="glyphicon glyphicon-send"></i></button>
						</form>
					</article>
				</section>
				<section class="col-sm-8">
					<div class="row"><div id="forecast-graph"></div></div>
				</section>
			</div>
		</main>
		<% include ../partials/footer %>
	</body>
	<% include ../partials/scripts %>
</html>