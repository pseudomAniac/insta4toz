<!DOCTYPE html>
<html lang="en" ng-app="WeatherReports">
	<head>
		<% include ../partials/head %>
		<!-- <% /* include ../partials/fb-pixels */ %> -->
	</head>
	<body class="container-fluid">
		<div id="fb-root"></div>
		<script type="text/javascript" src="/scripts/facebook-integration.js"></script>
		<main class="row">
			<div class="">
				<section class="col-sm-4 well-sm weather-data-widget">
					<% include ../partials/header %>
					<article class="row text-center">
						<!-- <figure class="col-sm-4 pull-left">
							<img class="pull-left img-responsive" src="../img/app-logo.png" title="application logo" />
						</figure> -->
						<h3 id="fb-welcome" class="col-sm-12"></h3>
						<div class="col-sm-12">
							<div id="fb-propic" class="col-sm-12"></div>
							<div class="col-sm-12"><% include ../partials/fb-like %></div>
						</div>
					</article>
					<article id="weather-info" class="row text-primary bg-warning text-center">
						<div class="col-sm-10 col-sm-offset-1">
							<p>On <%= weda.dt %> hours at <%= weda.name %>, the ground temperature was <%= weda.main.temp %> &deg; Celsius. with <%= weda.clouds.all %>% of the sky covered with <%= weda.weather[0].description%>.</p>
							<p>The wind averaged speeds of up to <%= weda.wind.speed %> km/h blowing in from the North at <%= weda.wind.deg %> &deg;</p>
							<p>The atmospheric pressure was <%= weda.main.pressure %> hPa and <%= weda.main.humidity %>% of the air was humid.</p>
						</div>
					</article>
					<article class="row">
						<section class="col-sm-12 ">
							<table class="table table-hover table-striped text-center">
								<thead>
									<tr>
										<th>latitude</th>
										<th>longitude</th>
										<th>sunrises</th>
										<th>sunsets</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td><%= weda.coord.lat %></td>
										<td><%= weda.coord.lon %></td>
										<td class="date-sunrise"><%= weda.sys.sunrise %></td>
										<td class="date-sunset"><%= weda.sys.sunset %></td>
									</tr>
								</tbody>
							</table>
							<p id="fb-location" class="col-sm-12 text-muted small text-center"></p>
						</section>
					</article>
				</section>
				<section class="col-sm-8">
					<div class="row">
						<div id="forecast-graph">
							<svg></svg>
						</div>
					</div>
				</section>
			</div>
		</main>
		<!-- <% include ../partials/footer %> -->
	</body>
	<% include ../partials/scripts %>
</html>